name: rentool
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: 
    sdk: flutter
  
  rentool_sdk:
    git: git@github.com:ElforL/Rentool_sdk.git
  
  # UI
  flutter_icons: ^1.1.0

  # Firebase
  firebase_core: ^1.3.0
  cloud_firestore: ^2.2.2
  firebase_auth: ^1.4.1
  firebase_storage: ^8.1.3
  firebase_messaging: ^10.0.4

  # Social auth
  google_sign_in: ^5.0.3
  flutter_facebook_auth: ^3.4.1

  # Misc
  image_picker: ^0.8.0+1
  video_player: ^2.1.5
  file_picker: ^3.0.2+2
  mime: ^1.0.0
  
  # // Needs an apple developer account which costs $99 🤷‍♂️
  # sign_in_with_apple: ^3.0.0
  # crypto: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  generate: true